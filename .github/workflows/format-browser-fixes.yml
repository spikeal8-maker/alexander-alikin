name: Format browser fixes

on:
  push:
    branches:
      - agent/recover-visible-site

permissions:
  contents: write

jobs:
  format:
    if: ${{ !contains(github.event.head_commit.message, '[skip browser format]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/recover-visible-site
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: npm
      - run: npm ci
      - run: npx prettier --write src/layouts/BaseLayoutV3.astro scripts/browser-checks/v2-design-tokens.mjs
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/layouts/BaseLayoutV3.astro scripts/browser-checks/v2-design-tokens.mjs
          if git diff --cached --quiet; then
            echo "No formatting changes"
          else
            git commit -m "style: format browser fixes [skip browser format]"
            git push origin HEAD:agent/recover-visible-site
          fi