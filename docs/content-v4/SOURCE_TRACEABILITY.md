# Трассировка источников CONTENT-EXPORT-001

Источник анализа зафиксирован неизменяемо:

- private repository: `spikeal8-maker/alexander-alikin-vault`;
- Draft PR: `#6`;
- source HEAD: `566eeb8266a8983f068443fe3c40693846ddcbbb`;
- дата проверки: `2026-07-27`;
- проверенный scope: ровно 29 файлов `site-content-v1`, перечисленных в
  `PUBLIC_CONTENT_MANIFEST.yaml`.

Приватный PR не сливался. Репозиторий, история Git, исходные файлы, вложения и
сырой vault в публичный репозиторий не копировались.

## Метод

Каждый разрешённый файл прочитан целиком и получил отдельную запись manifest.
Исходный статус сохраняется как публичный gate:

- `owner_review` не означает согласие владельца на публикацию;
- `fact_check` не может быть понижен до авторского утверждения;
- служебные карты без экспортного статуса считаются `blocked`;
- только `ready_for_public_export` разрешает создать публичный content target.

В source reference используется форма
`private-pr6:<точный путь внутри разрешённого scope>`. Она даёт происхождение,
но не раскрывает приватный текст.

## Результат трассировки

| Группа | Файлов | Ready | Решение |
|---|---:|---:|---|
| Governance и evidence | 3 | 0 | Реестр и проверки, без экспорта фактов |
| Identity / pages | 9 | 0 | Owner review или fact check |
| Practice / projects | 7 | 0 | Owner review или fact check |
| Journal | 8 | 0 | Owner review или fact check |
| Media | 2 | 0 | Blocked до инвентаря прав |
| **Всего** | **29** | **0** | **Публичных content entries не создано** |

Контрольные суммы приватных файлов намеренно не публикуются: source HEAD и
точные пути достаточны для воспроизводимой проверки внутри приватного доступа.
