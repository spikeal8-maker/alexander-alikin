---
import { buildUrl } from "@config/site";
---

<section class="v2-section">
  <div class="v2-container v2-fcta">
    <h2>Проверить, есть ли предмет для работы</h2>
    <p>
      Опишите текущий процесс, желаемое изменение и то, как вы поймёте, что первый этап оказался
      полезным. Условия сотрудничества определяются после разбора контекста.
    </p>
    <a href={buildUrl("/collaboration/")} class="v2-btn v2-btn-accent">Обсудить задачу</a>
  </div>
</section>

<style>
  .v2-fcta {
    text-align: center;
    max-width: 42rem;
  }
  .v2-fcta h2 {
    font-size: var(--text-4xl);
    color: var(--color-white);
    margin: 0 0 var(--space-6);
    font-weight: var(--fw-bold);
  }
  .v2-fcta p {
    font-size: var(--text-lg);
    color: rgba(255, 255, 255, 0.7);
    margin: 0 0 var(--space-8);
    line-height: var(--leading-normal);
  }
  @media (max-width: 768px) {
    .v2-fcta h2 {
      font-size: var(--text-2xl);
    }
    .v2-fcta p {
      font-size: var(--text-base);
    }
  }
</style>
