<section class="v2-section v2-section-surface">
  <div class="v2-container">
    <h2 class="v2-section-title">Проблема и диагностика</h2>
    <div class="v2-pd-split">
      <div class="v2-pd-signals">
        <div class="v2-pd-signal">
          <span class="v2-pd-signal-num">01</span>
          <h3>Процессы разрослись</h3>
          <p>Операции делаются вручную, передача между сотрудниками ломается, данные теряются.</p>
        </div>
        <div class="v2-pd-signal">
          <span class="v2-pd-signal-num">02</span>
          <h3>Решения не оцифрованы</h3>
          <p>
            Выбор делается на основании интуиции, а не данных. Нет единой картины происходящего.
          </p>
        </div>
        <div class="v2-pd-signal">
          <span class="v2-pd-signal-num">03</span>
          <h3>Масштабирование буксует</h3>
          <p>
            То, что работало на 10 клиентов, не работает на 100. Нужна архитектура, а не заплатки.
          </p>
        </div>
      </div>
      <div class="v2-pd-diag">
        <h3>Что показывает диагностика</h3>
        <ul>
          <li>Дублирование ручных операций в разных отделах</li>
          <li>Отсутствие единой структуры данных и источников правды</li>
          <li>Узкие места на стыках процессов и зон ответственности</li>
          <li>Технический долг, который тормозит любые изменения</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
  .v2-pd-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-12);
  }
  .v2-pd-signal {
    margin-bottom: var(--space-6);
  }
  .v2-pd-signal-num {
    display: inline-block;
    width: 1.75rem;
    height: 1.75rem;
    line-height: 1.75rem;
    text-align: center;
    border-radius: 50%;
    background: var(--color-accent-warm);
    color: var(--color-white);
    font-size: var(--text-xs);
    font-weight: var(--fw-bold);
    margin-right: var(--space-3);
  }
  .v2-pd-signal h3 {
    display: inline;
    font-size: var(--text-lg);
    color: var(--color-text);
  }
  .v2-pd-signal p {
    color: var(--color-text-secondary);
    margin: var(--space-2) 0 0 2.5rem;
  }
  .v2-pd-diag h3 {
    font-size: var(--text-xl);
    color: var(--color-text);
    margin: 0 0 var(--space-4);
  }
  .v2-pd-diag ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .v2-pd-diag li {
    padding: var(--space-3) 0;
    color: var(--color-text-secondary);
    border-bottom: var(--border-width) solid var(--color-border);
    font-size: var(--text-base);
  }
  .v2-pd-diag li::before {
    content: "\2014 ";
    color: var(--color-accent);
  }
  @media (max-width: 768px) {
    .v2-pd-split {
      grid-template-columns: 1fr;
    }
  }
</style>
