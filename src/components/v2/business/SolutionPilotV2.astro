<section class="v2-section">
  <div class="v2-container">
    <p class="v2-section-kicker">Первый рабочий контур</p>
    <h2 class="v2-section-title">Что проверяется в пилоте</h2>
    <div class="v2-sp-grid">
      <div class="v2-sp-col">
        <div class="v2-sp-item">
          <h3>Архитектура процесса</h3>
          <p>
            Фиксируем роли, данные, переходы между этапами и места, где решение теряет
            управляемость.
          </p>
        </div>
        <div class="v2-sp-item">
          <h3>Ограниченная автоматизация</h3>
          <p>
            Выбираем один участок, где интеграция, правило или ИИ-инструмент могут дать проверяемое
            изменение.
          </p>
        </div>
        <div class="v2-sp-item">
          <h3>Передача команде</h3>
          <p>
            Решение документируется и объясняется людям, которые будут использовать и развивать его
            дальше.
          </p>
        </div>
      </div>
      <div class="v2-sp-col">
        <div class="v2-sp-criteria">
          <h3>Критерии первого этапа</h3>
          <ul>
            <li>Есть описание текущего и целевого процесса</li>
            <li>Определён владелец процесса со стороны заказчика</li>
            <li>Выбрана одна проверяемая гипотеза</li>
            <li>Зафиксирован способ сравнить состояние до и после</li>
          </ul>
        </div>
        <div class="v2-sp-limits">
          <h3>Честные ограничения</h3>
          <ul>
            <li>Срок и объём определяются только после диагностики</li>
            <li>Пилот не равен автоматизации всей компании</li>
            <li>Без участия команды заказчика решение не передаётся в эксплуатацию</li>
            <li>Коммерческие сроки и фиксированные показатели пока не заявляются публично</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .v2-section-kicker {
    margin: 0 0 var(--space-2);
    color: var(--color-accent-text);
    font-size: var(--text-sm);
    font-weight: var(--fw-bold);
  }
  .v2-sp-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-12);
  }
  .v2-sp-col {
    min-width: 0;
  }
  .v2-sp-item {
    margin-bottom: var(--space-6);
    padding-bottom: var(--space-6);
    border-bottom: var(--border-width) solid var(--color-border-light);
  }
  .v2-sp-item h3,
  .v2-sp-criteria h3,
  .v2-sp-limits h3 {
    font-size: var(--text-lg);
    margin: 0 0 var(--space-3);
    color: var(--color-white);
  }
  .v2-sp-item p,
  .v2-sp-criteria li,
  .v2-sp-limits li {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.72);
    line-height: var(--leading-normal);
  }
  .v2-sp-item p {
    margin: 0;
  }
  .v2-sp-criteria,
  .v2-sp-limits {
    border-radius: var(--radius-lg);
    padding: var(--space-6);
  }
  .v2-sp-criteria {
    background: rgba(37, 99, 235, 0.1);
    border: var(--border-width) solid rgba(37, 99, 235, 0.3);
    margin-bottom: var(--space-6);
  }
  .v2-sp-limits {
    background: rgba(245, 158, 11, 0.06);
    border: var(--border-width) solid rgba(245, 158, 11, 0.22);
  }
  .v2-sp-criteria ul,
  .v2-sp-limits ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .v2-sp-criteria li,
  .v2-sp-limits li {
    padding: var(--space-2) 0;
  }
  .v2-sp-criteria li::before {
    content: "✓\00a0";
    color: var(--color-accent-text);
    font-weight: var(--fw-bold);
  }
  .v2-sp-limits li::before {
    content: "—\00a0";
    color: var(--color-accent-warm);
  }
  @media (max-width: 768px) {
    .v2-sp-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
