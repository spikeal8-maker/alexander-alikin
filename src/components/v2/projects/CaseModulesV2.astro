<section class="v2-section">
  <div class="v2-container">
    <p class="v2-accent-label">Проектная структура</p>
    <h2 class="v2-section-title">Основные направления модулей</h2>
    <div class="case-modules-grid">
      <div class="case-module-card">
        <h3>Виртуальные лаборатории</h3>
        <p>
          Среда для практических заданий и симуляций. Текущий объём реализации требует отдельного
          описания.
        </p>
      </div>
      <div class="case-module-card">
        <h3>Управление группами</h3>
        <p>Роли, назначения и движение учебной работы внутри курса или класса.</p>
      </div>
      <div class="case-module-card">
        <h3>Проектная среда</h3>
        <p>Организация учебных проектов, результатов и обратной связи.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .case-modules-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }
  .case-module-card {
    border-left: 3px solid var(--color-primary);
    padding-left: var(--space-6);
  }
  .case-module-card h3 {
    font-size: var(--text-xl);
    color: var(--color-white);
    margin: 0 0 var(--space-3);
  }
  .case-module-card p {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.7);
    line-height: var(--leading-normal);
    margin: 0;
  }
  @media (max-width: 768px) {
    .case-modules-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
